version: 0.2
phases:
  pre_build:
    commands:
      - echo "Setting up Terraform..."
  build:
    commands:
      - echo "Applying Terraform to create/update EKS cluster..."
      - sudo chmod +x ./scripts/terraform-eks.sh
      - sudo ./scripts/terraform-eks.sh
  post_build:
    commands:
      - echo "Build Complete"